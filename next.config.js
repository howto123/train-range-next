/** @type {import('next').NextConfig} */
const nextConfig = {
    //productionBrowserSourceMaps: true,
    //distDir: 'dist',
    //output: 'export'
    output: 'standalone',
}

module.exports = nextConfig